server:
  port: 8082

spring:
  application:
    name: restaurant-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/foodorder_restaurant}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
